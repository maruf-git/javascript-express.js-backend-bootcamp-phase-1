// frontend -> website/app user interface (resturent menu) -> request -> receive: cooked food (response)
// waiter (api) -> request carry - response cary
// backend -> website/app internal logical operations (checf) -> works on request -> delivers response(cooked food)

// server -> computer -> processer-> work ->(logical work)

// api
// rest api= Representational State Transfer
// http
// crud operations
// create ->post() ->data create(database e kichu data insert kora)
// read ->get() ->data read (database theke kichu data read kore userend send kora)
// update ->put()/patch()-> data update (database a data update kora)
// delele ->delete()-> data delete (kono akta data chilo seta delete kora)

// web server